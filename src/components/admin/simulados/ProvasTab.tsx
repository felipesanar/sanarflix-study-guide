/** Fatia C1 — sub-aba Provas (tabela de simulados, config, questões, anular). */
import { AdminEmpty } from '@/experiences/admin/ui';

export default function ProvasTab() {
  return <AdminEmpty title="Provas" description="Em construção (fatia C1)." />;
}
