/** Fatia C2 — sub-aba Importar respostas (wizard BulkRunner sobre a edge function existente). */
import { AdminEmpty } from '@/experiences/admin/ui';

export default function ImportarRespostasTab() {
  return <AdminEmpty title="Importar respostas" description="Em construção (fatia C2)." />;
}
