import * as React from 'react';
import { StudyGuideImportTab } from '@/components/admin/StudyGuideImportTab';

/** Seção Guia (importação do guia de estudos) do Portal do Admin (`/admin/guia`). */
const GuiaPage: React.FC = () => <StudyGuideImportTab />;

export default GuiaPage;
