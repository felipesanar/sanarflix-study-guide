import * as React from 'react';
import { AnnouncementsTab } from '@/components/admin/AnnouncementsTab';

/** Seção Avisos do Portal do Admin (`/admin/avisos`). */
const AvisosPage: React.FC = () => <AnnouncementsTab />;

export default AvisosPage;
