import * as React from 'react';
import { AlunosRiscoScreen } from '@/components/gestor/alunos-risco/AlunosRiscoScreen';

/** Alunos & risco do gestor (`/gestor/alunos-risco`). */
const AlunosRiscoPage: React.FC = () => <AlunosRiscoScreen />;

export default AlunosRiscoPage;
