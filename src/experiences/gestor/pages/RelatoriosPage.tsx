import * as React from 'react';
import { RelatoriosModule } from '@/components/gestor/relatorios/RelatoriosModule';

/** Relatórios do gestor (`/gestor/relatorios`). */
const RelatoriosPage: React.FC = () => <RelatoriosModule />;

export default RelatoriosPage;
